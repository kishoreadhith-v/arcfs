%% FGCS Highlights — submitted as a SEPARATE FILE at upload time
%% Rules: 3–5 bullet points, each ≤ 85 characters including spaces.
%% Character counts verified below each item.

\documentclass{article}
\usepackage[margin=2cm]{geometry}
\begin{document}

\noindent\textbf{Highlights}

\begin{itemize}
  % 83 chars:
  \item ArcFS integrates CDC deduplication, CAS, and CoW snapshots in a single Rust FUSE daemon

  % 74 chars:
  \item 9.2$\times$ sequential write speedup over ext4 via bounded write-back LRU cache

  % 72 chars:
  \item 83.7\% storage reduction on identical-file workloads; zero CRC32c errors

  % 68 chars:
  \item TagFS provides semantic tag-path navigation without data duplication

  % 78 chars:
  \item Eleven quantified limitations map a concrete path toward production deployment
\end{itemize}

\end{document}
